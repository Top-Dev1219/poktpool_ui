export const POKTPOOL_STRING = 'poktpool™'

// Auth expiration configuration (in milliseconds)
export const AUTH_CONFIG = {
  ACCESS_TOKEN_EXPIRES_IN: 3600, // 1 hour in milliseconds
  JWT_MAX_AGE: 60 * 29, // 29 minutes in seconds
}