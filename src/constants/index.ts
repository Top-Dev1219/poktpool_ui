export const POKTPOOL_STRING = 'poktpool™'

// Auth expiration configuration
export const AUTH_CONFIG = {
  // Access token expiration time in milliseconds (1 hour)
  ACCESS_TOKEN_EXPIRES_IN: 3600,
  // JWT max age in seconds (29 minutes)
  JWT_MAX_AGE: 60 * 29,
  // Session timeout in minutes (29 minutes)
  SESSION_TIMEOUT_MINUTES: 29,
  // Countdown timer in seconds (10 seconds)
  LOGOUT_COUNTDOWN_SECONDS: 10,
}